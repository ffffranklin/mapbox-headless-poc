{
  "name": "mapbox-headless-poc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "vm:init": "vagrant up",
    "vm:start": "vagrant ssh -c \"source ~/.nvm/nvm.sh && cd /vagrant && npm run start\"",
    "start": "DEBUG=mapbox-headless-poc node server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "connect": "^3.6.6",
    "debug": "^3.1.0",
    "puppeteer": "^1.4.0",
    "serve-static": "^1.13.2"
  }
}
